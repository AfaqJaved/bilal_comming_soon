version: "3.8"

services:
  afaq:
    build:
      context: .
      dockerfile: Dockerfile
    #ports:
    #  - "8080:80"
    container_name: afaq_container
    restart: unless-stopped
    networks:
      - nginx

networks:
  nginx:
    external: true